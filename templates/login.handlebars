 {{{message}}}
<!-- <form method="post" action="/login">
    username: <input type="text" name="username">
    password: <input type="password" name="password">
    <input type="submit" name="submit">
</form>
-->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login and Sign Up</title>
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
    }

    .login-container {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .login-box {
      padding: 20px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      width: 380px;
      height: 300px;
      text-align: center;
      background-color: white;
    }
    
    .signup-box {
      padding: 20px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      width: 330px;
      height: 300px;
      text-align: center;
      background-color: #321fdb;
      color: white;
      /* Center text in signup-box */
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }

    .signup-box h1 {
      margin-bottom: 10px;
      /* Set color for text in signup-box */
      color: white;
    }

    p {
      margin-bottom: 20px;
      color: white;
    }

    form {
      display: flex;
      flex-direction: column;
    }

    input {
      margin-bottom: 10px;
      padding: 8px;
      border: 1px solid #ccc;
      border-radius: 4px;
    }

    /* Style for login button */
    .login-box button {
      padding: 10px;
      background-color: #321fdb;
      color: white;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }

    .login-box button:hover {
      background-color: #321fdb;
    }

    /* Set border outline color for register button to white */
    .signup-box button {
      padding: 10px;
      background-color: #321fdb;
      color: white;
      border: 1px solid white;
      border-radius: 4px;
      cursor: pointer;
    }

    .signup-box button:hover {
      background-color: #321fdb;
    }

    a {
      text-decoration: none;
      color: #321fdb;
      display: block;
      margin-top: 10px;
    }

    a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <div class="login-box">
      <h1 class="mylogin">Login</h1>
      <p class="black">Sign in to your account</p>
      <form method="post" action="/login">
        <input name="username" placeholder="Username">
        <input name="password" type="password" placeholder="Password">
        <button type="submit">Login</button>
      </form>
      <a href="/forgot">Forgot password?</a>
    </div>

    <div class="signup-box">
      {{! <h1>Sign up</h1>
      <p>Please sign up if you want to view the details of the petrol stations</p>
      <button class="register-button" type="submit">Register Now!</button> }}
    </div>
  </div>
</body>
</html>